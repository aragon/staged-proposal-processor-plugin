CanProposalAdvance_SPP_IntegrationTest
├── when existent proposal
│   ├── when minAdvance is not reached
│   │   └── it should return false.
│   ├── when maxAdvance is reached
│   │   └── it should return false.
│   ├── when proposal is executed
│   │   └── it should return false.
│   ├── when minAdvance is lower than stageDuration
│   │   ├── when veto stage threshold is not zero  // ! dupplicated changed when for given until fixed in bulloak
│   │   │   ├── when stageDuration is not reached
│   │   │   │   └── it should return false.
│   │   │   └── when stageDuration is reached
│   │   │       ├── when veto threshold is met
│   │   │       │   └── it should return false.
│   │   │       └── when veto threshold is not met  //! dupplicated changed when for given until fixed in bulloak
│   │   │           ├── when approval threshold is met   //! dupplicated changed when for given until fixed in bulloak
│   │   │           │   └── it should return true.
│   │   │           └── when approval threshold is not met  //! dupplicated re-frased until fixed in bulloak
│   │   │               └── it should return false.
│   │   └── when veto stage threshold is zero  //! dupplicated changed when for given until fixed in bulloak
│   │       ├── when the approval threshold is met  //! dupplicated re-frased until fixed in bulloak
│   │       │   └── it should return true.
│   │       └── given approval threshold is not met  // ! dupp
│   │           └── it should return false.
│   └── when minAdvance is bigger than stageDuration
│       ├── when stageDuration is reached but minAdvance is not reached
│       │   └── it should return false.
│       └── when stageDuration and minAdvance are reached
│           ├── when veto stage threshold is not zero // ! dupp
│           │   ├── given veto threshold is met   // ! dupp
│           │   │   └── it should return false.
│           │   └── given veto threshold is not met  // ! dupp
│           │       ├── given approval threshold is met  // ! dupp
│           │       │   └── it should return true.
│           │       └── when the approval threshold is not met  // ! dupp
│           │           └── it should return false.
│           └── given veto stage threshold is zero  // ! dupp
│               ├── given the approval threshold is met  // ! dupp
│               │   └── it should return true.
│               └── given the approval threshold is not met // ! dupp
│                   └── it should return false.
└── when non existent proposal
    └── it should return false.  // TBD



// ! this tree file doens't work on bulloak, since it is not allowing duplicated function or modifiers names 
